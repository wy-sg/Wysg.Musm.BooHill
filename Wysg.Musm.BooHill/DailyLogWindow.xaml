<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Wysg.Musm.BooHill.DailyLogWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="일간 가져오기 로그">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid RowDefinitions="Auto,Auto,*" Padding="24" RowSpacing="16">
        <!-- Header -->
        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBlock Text="일간 가져오기 로그" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" />
            <CalendarDatePicker x:Name="DatePicker" PlaceholderText="날짜 선택"
                                DateChanged="DatePicker_DateChanged" />
            <TextBlock x:Name="TotalLinesText" VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="13" />
        </StackPanel>

        <!-- Infographic summary cards -->
        <Grid Grid.Row="1" ColumnSpacing="12" RowSpacing="12" x:Name="SummaryGrid" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- PARSE card -->
            <Border Background="#E3F2FD" CornerRadius="12" Padding="16" MinHeight="120">
                <StackPanel Spacing="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="🔍" FontSize="28" HorizontalAlignment="Center" />
                    <TextBlock Text="파싱" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" />
                    <TextBlock x:Name="ParseCountText" Text="0회" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#1565C0" />
                    <TextBlock x:Name="ParseDetailText" Text="" FontSize="11" HorizontalAlignment="Center" Foreground="#42A5F5" TextAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- IMPORT_DUP card -->
            <Border Grid.Column="1" Background="#FFF3E0" CornerRadius="12" Padding="16" MinHeight="120">
                <StackPanel Spacing="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="📥" FontSize="28" HorizontalAlignment="Center" />
                    <TextBlock Text="중복 가져오기" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" />
                    <TextBlock x:Name="DupCountText" Text="0건" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#E65100" />
                    <TextBlock x:Name="DupDetailText" Text="" FontSize="11" HorizontalAlignment="Center" Foreground="#FB8C00" TextAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- MERGE card -->
            <Border Grid.Column="2" Background="#E8F5E9" CornerRadius="12" Padding="16" MinHeight="120">
                <StackPanel Spacing="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="🔗" FontSize="28" HorizontalAlignment="Center" />
                    <TextBlock Text="합치기" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" />
                    <TextBlock x:Name="MergeCountText" Text="0건" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#2E7D32" />
                    <TextBlock x:Name="MergeDetailText" Text="" FontSize="11" HorizontalAlignment="Center" Foreground="#66BB6A" TextAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- INSERT_NEW card -->
            <Border Grid.Column="3" Background="#F3E5F5" CornerRadius="12" Padding="16" MinHeight="120">
                <StackPanel Spacing="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="🏠" FontSize="28" HorizontalAlignment="Center" />
                    <TextBlock Text="새 주택" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" />
                    <TextBlock x:Name="NewCountText" Text="0건" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#6A1B9A" />
                    <TextBlock x:Name="NewDetailText" Text="" FontSize="11" HorizontalAlignment="Center" Foreground="#AB47BC" TextAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Detail list -->
        <Grid Grid.Row="2" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8"
              BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Padding="12" Background="{ThemeResource LayerFillColorAltBrush}">
                <TextBlock Text="상세 로그" FontWeight="SemiBold" />
            </StackPanel>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ListView x:Name="LogEntryList" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="{Binding LeftPadding}" ColumnSpacing="10" Opacity="{Binding RowOpacity}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Timestamp}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                                <Border Grid.Column="1" Background="{Binding ActionColor}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding ActionLabel}" FontSize="11" FontWeight="{Binding LabelWeight}" />
                                </Border>
                                <TextBlock Grid.Column="2" Text="{Binding Detail}" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="{Binding DetailFontSize}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>

            <!-- Empty state -->
            <TextBlock x:Name="EmptyText" Grid.Row="1" Text="날짜를 선택하세요" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="16" />
        </Grid>
    </Grid>
</Window>
